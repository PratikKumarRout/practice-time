// Optional: Add a small delay before showing the typing effect
document.addEventListener("DOMContentLoaded", () => {
  const typing = document.getElementById("typing");
  typing.style.animationPlayState = "running";
});
